import 'package:flutter/material.dart';

class GoalListBuilder extends StatelessWidget {
  const GoalListBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
